#include<stdio.h>

int find_min_dis(int a[],int b[],int c[]){
    int x=0,y=0,z=0;
    int alen=sizeof(a)/4,blen=sizeof(b)/4,clen=sizeof(c)/4;
    while (x!=alen||y!=blen||z!clen)
    {
        
    }
    
    return 1;
}

int main(){

    return 0;
}