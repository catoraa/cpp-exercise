#include<stdio.h>

typedef struct TreeNode {
      int val;
      TreeNode *left;
      TreeNode *right;
}TreeNode;

void flatten(struct TreeNode* root){
    if(root->left->left==NULL&&root->left->left==NULL) 
}


int main(){
    return 0;
}